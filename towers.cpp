#include<bits/stdc++.h>
using namespace std;
#define int long long int
int32_t main(){
    int n;
    cin>>n;
    map<int ,int> mymap;
    int towers=0;
    for(int i=0;i<n;i++){
        int k;
        cin>>k;
        if(mymap.find(k)!=mymap.end()){

        }
    }

return 0;
}
